# Talkbook 统一设计规范

## 1. 设计基准

本文件是 Talkbook 微信小程序唯一设计规范。

以下内容共同组成最终定稿来源，但以本文件的规则解释为准：

- `ui/*/screen.png`
- `ui/*/code.html`

如果页面实现、Stitch 生成稿、旧设计文档之间有冲突，本文件优先。

## 2. 产品定位

Talkbook 不是展示型概念稿，也不是内容社区。
它必须像一个已经上线、真实可用的微信小程序产品。

目标感受：

- 温暖
- 平静
- 可信
- 易读
- 有产品完成度

目标用户优先面向中老年记录者、家庭故事讲述者，以及想先开口再成稿的普通创作者。

## 3. 总体原则

### 3.1 产品优先，不做海报感设计

- 页面首先要像工具，而不是视觉概念板。
- 不做夸张 hero 海报布局。
- 不做抢内容注意力的装饰性渐变。
- 不做玻璃拟态。
- 不做脱离真实交互的“编辑部大片感”页面。

### 3.2 微信小程序心智优先

- 这是微信小程序，不是原生 App 概念图。
- 头部、按钮密度、留白、页面层级都要贴近真实小程序。
- 页面应实用、紧凑、清晰，而不是展示型画板。

### 3.3 适老可读性优先

- 正文和重要说明要大字号、易扫读。
- 点击区域要足够大。
- 不依赖纯图标表达关键动作。
- 重要信息必须有清晰中文标签。

### 3.4 统一中文界面

- 所有可见文案必须是自然的简体中文。
- 绝不能出现原始 token 字符串。

禁止示例：

- `auto_stories`
- `person_book`
- `history_edu`
- `family_history`
- `edit_note`
- `play_circle`
- `pause`
- `stop`
- `mic`
- `info`
- `chevron_left`

如果图标渲染不稳定，就移除图标，只保留中文文字。

## 4. 视觉系统

### 4.1 色彩

主视觉采用暖陶土色体系。

- 主色：陶土棕
- 次色：暖桃 / 浅砂色
- 背景：暖白
- 点缀：低饱和琥珀
- 状态色：仅在必要时使用柔和绿色或红色

色彩规则：

- 所有页面主色调保持一致，不允许某页突然偏蓝、偏绿、偏灰。
- 状态色只表达状态，不参与主体氛围。
- 大面积内容区以暖白和浅暖底色为主，避免高对比硬切。

建议色板：

- `primary`: `#9B3F1E`
- `primary-soft`: `#BB5733`
- `secondary-container`: `#FFBDA7`
- `surface`: `#FFF8F6`
- `surface-low`: `#FFF1ED`
- `surface-card`: `#FDEAE4`
- `surface-card-strong`: `#F8E4DE`
- `text-primary`: `#231916`
- `text-secondary`: `#56423C`
- `outline-soft`: `#DDC0B8`

### 4.2 版式与层次

- 用留白和底色层次分组，少用线。
- 尽量避免 1px 分割线作为主结构。
- 卡片、模块、按钮圆角统一。
- 轻阴影即可，不做厚重悬浮感。

### 4.3 字体

- 使用一套干净的无衬线体系。
- 标题需要足够稳、够粗，作为页面锚点。
- 正文要平静、可读，不做过细字重。
- 中文环境下保持自然字距，不做刻意“英文化”的大字报排版。

## 5. 导航规范

### 5.1 顶部栏

所有页面顶部栏统一规则：

- 标题居中
- 高度一致
- 左右区域视觉平衡

页面规则：

- 首页
  - 中间标题：`Talkbook`
  - 左侧留空
  - 右侧：`微信登录` 或头像入口
- 访谈页
  - 左侧：`返回`
  - 中间标题：`智能访谈`
  - 右侧留空
- 书稿预览页
  - 左侧：`返回`
  - 中间标题：`书稿预览`
  - 右侧留空
- 章节预览页
  - 左侧：`返回`
  - 中间标题：当前章节名
  - 右侧留空
- 我的书稿页
  - 这是 root 页
  - 无返回
  - 中间标题：`我的书稿`
  - 右侧留空

禁止：

- 无说明菜单图标
- 无定义设置图标
- 某些页左对齐标题、某些页居中标题

### 5.2 底部导航

底部导航只出现在 root 页面：

- 创作页
- 我的书稿页

统一要求：

- 高度一致
- 激活态一致
- 文案一致
- 内边距一致

标签固定为：

- `创作`
- `我的书稿`

如果图标不稳定，允许仅用中文标签，但激活态必须清晰。

访谈页、书稿预览页、章节预览页不显示底部导航。

## 6. 登录规范

### 6.1 首页登录入口

首页必须有明确的微信登录入口：

- 文案：`微信登录`
- 带微信识别图形
- 图形和文字作为一个整体入口出现

禁止：

- 用设置入口替代登录
- 把登录藏成弱提示链接
- 仅展示品牌标题，没有清晰登录动作

### 6.2 已登录状态

登录后：

- `微信登录` 替换为头像或用户入口
- 顶部栏布局保持稳定
- 不自动引入设置图标

当前开发阶段：

- 允许使用 mock 登录态
- 视觉和交互按真实微信登录入口设计

## 7. 页面级规范

### 7.1 创作页

页面目标：

- 解释产品价值
- 让用户选择书籍类型
- 让用户开始创作

必须包含：

- 顶部中间标题 `Talkbook`
- 右上角 `微信登录` 或头像
- 一句简洁价值说明
- 四个固定类型
  - 小说
  - 自传
  - 回忆录
  - 家族故事
- 单选态
- 主按钮 `开始创作`
- 最近书稿或创作进度提示
- 底部导航

禁止：

- `查看更多`
- 额外类型入口
- 菜单图标
- 设置图标
- 海报式大面积装饰

### 7.2 访谈页

页面目标：

- 承接 AI 采访
- 支持清晰的录音与文字回看

必须包含：

- 左侧 `返回`
- 中间标题 `智能访谈`
- 当前问题
- `语音播放 / 文字记录` 切换
- 带时间的消息时间线
- 空闲态按钮 `开始录音`
- 录音中动作
  - `暂停录音`
  - `结束录音`
- 明确的录音状态
  - `正在录音`
  - 当前时长
- 长录音自动分段提示
- `跳过这一问`
- `生成书稿预览`

禁止：

- `完成`
- 第三个录音主动作
- 原始图标 token 文本

录音分段使用中文标签，例如：

- `第1段`
- `第2段`

### 7.3 书稿预览页

页面目标：

- 让用户清楚看到“采访内容已经变成结构化书稿”

必须包含：

- 左侧 `返回`
- 中间标题 `书稿预览`
- `书名建议`
- `一句话简介`
- `章节目录`
- 每章摘要
- `完整版待解锁`
- 主按钮 `继续补充素材`
- 次按钮 `分享`

页面感受应像内容详情页，而不是封面展示页。

### 7.4 章节预览页

页面目标：

- 展示单章内容，让用户感受到“成书”的质感

必须包含：

- 左侧 `返回`
- 中间章节标题
- 当前章节标记，例如 `第一章`
- 大标题
- 连续正文阅读区
- 底部章节切换
  - `上一章`
  - 当前主动作
  - `下一章`

禁止：

- 无定义更多菜单
- 无意义悬浮图标
- 编辑器式复杂工具栏

如果当前版本不支持真正编辑，底部主动作应替换为明确中文动作，例如 `继续补充素材`。

### 7.5 我的书稿页

页面目标：

- 管理已创建书稿

必须包含：

- 中间标题 `我的书稿`
- 当前书稿数量
- 实用型列表卡片
- 卡片字段
  - 标题
  - 状态
  - 更新时间
  - 下一动作，例如 `继续创作` 或 `预览`
- `开启新书`
- 低优先级工具项
  - `会员权益`
  - `订单记录`
  - `版权与协议`
- 底部导航

禁止：

- 书架拟物
- 假封面画廊
- 装饰大于信息

## 8. 组件规范

### 8.1 按钮

主按钮：

- 实底暖陶土色
- 满宽或明显主导
- 点击区域大

次按钮：

- 描边或浅底
- 不能和主按钮抢层级

### 8.2 状态标签

- 紧凑
- 一致
- 不抢内容主体

### 8.3 卡片

- 统一圆角
- 轻阴影或纯底色层次
- 信息优先，不做复杂装饰

## 9. Stitch 与实现规则

虽然设计已经定稿，但后续任何参考生成都必须遵循：

- 以本文件为唯一规则来源
- `ui/*/screen.png` 为视觉参考
- `ui/*/code.html` 为结构参考
- 正确性优先于新奇感
- 如果图标不稳定，就退化为纯中文文本

## 10. 验收检查

在接受页面前，逐项检查：

- root 页面底部导航样式一致
- 顶部标题都居中
- 没有原始 token 字符串
- 首页有清晰微信登录入口
- 访谈页录音操作只有 `开始录音`、`暂停录音`、`结束录音`
- 首页没有 `查看更多`
- 没有无定义菜单或设置入口
- 全部文案是自然简体中文
- 页面看起来像真实产品，而不是设计概念图
